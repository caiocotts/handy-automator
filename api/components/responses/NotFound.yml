description: Not Found
content:
  application/json:
    schema:
      type: object
      properties:
        message:
          type: string
          example: "unable to find requested record"
      required:
        - message
