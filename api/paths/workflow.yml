get:
  summary: Get all workflows
  operationId: getWorkflows
  description: Get a list of all workflows in the system.
  security:
    - accessToken: [ ]
  responses:
    '200':
      description: Workflow records successfully retrieved
      content:
        application/json:
          schema:
            type: object
            properties:
              workflows:
                $ref: ../components/schemas/WorkflowList.yml
post:
  summary: Create a workflow
  operationId: createWorkflow
  description: Create a new workflow. For adding devices to the workflow see <a href=#operation/associateWorkflowDevices>associateWorkflowDevices</a>.
  security:
    - accessToken: [ ]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            name:
              type: string
              description: Workflow name
              example: Turn on bedroom lights
          required:
            - name
  responses:
    '201':
      description: Workflow record successfully created
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Workflow.yml
    '400':
      $ref: ../components/responses/BadRequest.yml
