get:
  summary: Check Decision Maker's status
  operationId: ping
  responses:
    '200':
      description: Decision Maker is up and running
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Pong.yml
