get:
  summary: Check Decision Maker's status
  operationId: ping
  responses:
    '200':
      description: Decision Maker is up and running
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: string
                example: ok
            required:
              - status
